import RPi.GPIO as GPIO
import datetime

GPIO.setwarnings (0)
GPIO.setmode(GPIO.BCM)
GPIO.setup(21, GPIO.OUT)
GPIO.output(21, 0)

hourON 
minuteON
hourOff
minuteOFF

d = datetime = datetime.now()
    x = 

    
                